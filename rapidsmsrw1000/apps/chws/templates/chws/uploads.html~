{% extends base_template %}

{% block content %}
    {% load pagination-tags %}


<table width="100%"> 
    <thead>
        <tr>
        	<th>Number of CHWs Uploaded</th>
		<th>Number of CHWs confirmed</th>
		<th>Number of CHWs pending</th>
            <th>&nbsp;</th>
            <th>&nbsp;</th>
            
        </tr>
    </thead>
    <tbody>
    	
            <tr>
            	
                <td><a href="/admin/chws/reporter">{{regs.count}}</a></td>
                <td><a href="/chws/confirms">{{confirms.count}}</a></td>
		<td><a href="/chws/pendings">{{pendings.count}}</a></td>
		<td><a href="/chws/import">Upload New List</a></td>
		<td><a href="/chws/uploads?excel{%if request.GET.province%}&province={{request.GET.province}}{%endif%}{%if request.GET.district%}&district={{request.GET.district}}{%endif%}{%if request.GET.facility%}&facility={{request.GET.facility}}{%endif%}">Export</a></td>
            </tr>
        
    </tbody>
    
    <tfoot>
			<tr>
				<td colspan="10">
					
				</td>
			</tr>
			<tr>
				<td colspan="10">
				
				</td>
			</tr>
	</tfoot>
</table>


<table width="100%"> 
    <thead>
        <tr>
        	
            	<th>Upload Reference</th>
		<th>District</th>
		<th>When</th>
		<th>By</th>
		
            
            
        </tr>
    </thead>
    <tbody>
    	{% for error in errors.object_list %}
            <tr>
            	
                
		<td><a href="/chws/view/errors/{{error.upload_ref}}">{{error.upload_ref}}</a></td>
		<td>{{error.district}}</td>
		<td>{{error.when}}</td>
		<td>{{error.by}}</td>
		
            </tr>
        {% endfor %}
    </tbody>
    
    <tfoot>
			<tr>
				<td colspan="10">
					{% paginator errors %}
				</td>
			</tr>
			<tr>
				<td colspan="10">
				Matching Errors: {{ errors.paginator.count }}
				</td>
			</tr>
	</tfoot>
</table>


{% endblock %}


